## Lab 1- MSE 245

I learned about the role of an API (Application Programming Interface) and its role as the communicator between the backend and front end. I learned that it is need for the fornt end to be able to send requests to the backend and that they do not communicate dircetly (which is what I thought before the class). 

I also learned that some classing types include:

- The `typeof` operator returns a string describing a value's type
- `typeof null` returns `"object"` even though `null` is its own type
- There are eight types in total, including `undefined` and `symbol`

```javascript
let x
0.7*0.2
x=5/2
typeof null

```

```
undefined
0.13999999999999999
2.5
'object'

```

